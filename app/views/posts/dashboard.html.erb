<table class = "table">
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>
    <tr>
      <th>Titulo</th>
      <th>Fecha creación</th>
      <th>URL Imagen</th>
      <th>Comentario</th>
    </tr>
  <tbody>
    <% @posts.each do |post| %>
        <tr>
            <td><%= post.title%></td>
            <td><%= post.created_at%></td>
            <td><img src=<%= post.image_url%>></td>
            <td><%= post.content%></td>
        </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'New Post', new_post_path %>